version: '3'
services:
  bot:
    build: .
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_ID=${ADMIN_ID}
      - DB=/app/data/bot.db
    volumes:
      - bot_data:/app/data
    restart: always

volumes:
  bot_data: